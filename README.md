# Docs Site Template

This is the shared template for ecosystem `docs.<domain>` sites.

DO NOT edit this directly for a per-site change. Per-site customization belongs
in the consuming repo. Edits here propagate to all future docs sites.

See `~/opencastor-ops/docs/standards/docs-site-pattern.md` for the bootstrap
procedure.
